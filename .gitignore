# Binaries for programs and plugins
secure-p2p-messaging-quic
p2p-messenger
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories
# vendor/

# IDEs
.vscode/
.idea/

# SECURITY - CRITICAL
# Ignore TLS certificates and keys so I don't leak them
*.pem
*.key
*.crt

# TLS session key log – NEVER commit; contains secrets that decrypt traffic
tls_keys.log

# Log files
*.log
